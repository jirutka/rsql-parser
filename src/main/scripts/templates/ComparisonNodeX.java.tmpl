// generated by generateAst.groovy
package <%= packageName %>;

import java.util.List;

public final class <%= className %> extends ComparisonNode {

    public <%= className %>(String selector, List<String> arguments) {
        super(selector, arguments);
    }

    public String getOperator() {
        return "<%= operator %>";
    }

    public <%= className %> withSelector(String selector) {
        return new <%= className %>(selector, getArguments());
    }

    public <%= className %> withArguments(List<String> arguments) {
        return new <%= className %>(getSelector(), arguments);
    }

    public <R, A> R accept(RSQLVisitor<R, A> visitor, A param) {
        return visitor.visit(this, param);
    }
}
