// generated by generateAst.groovy
package <%= packageName %>;

/**
 * An adapter for the {@link RSQLVisitor} interface with a simpler contract
 * that omits an optional second argument.
 *
 * @param <R> Return type of the visitor's method.
 */
public abstract class <%= className %><R> implements RSQLVisitor<R, Void> {

<% nodesNames.each { name -> %>
    public abstract R visit($name node);
<% } %>
<% nodesNames.each { name -> %>
    @Override public R visit(<%= name %> node, Void param) {
        return visit(node);
    }
<% } %>
}
